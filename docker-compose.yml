version: '3.8'

services:
  hostping_bot:
    build: .
    container_name: hostping_bot
    restart: always
    env_file:
      - .env
    environment:
      - SCAN_INTERVAL_SECONDS=300
    volumes:
      - ./data:/app/data
      - ./logs:/app/logs
